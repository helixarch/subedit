# Put here key words or key phrases. Any subtitles in the subtitle file
# that contain them will be removed using the clean trash option (-c).
# Each line must contain one key word or key phrase.
# It is recommended that this file is saved with UTF-8 encoding.

